package com.sirket.platform.crm.activity.domain;

/**
 * The channels a customer interaction can be recorded through (FR-CRM-05).
 */
public enum ActivityType {
    CALL,
    EMAIL,
    MEETING,
    NOTE
}
